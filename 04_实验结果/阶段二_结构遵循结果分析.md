# Step2 漂移诊断报告

结论：
prompt2 在四个问题上均达到满分结构遵循；
prompt1 存在稳定的结构漂移模式。

主要漂移类型：
1. 事实段过长（引入建议/解释）
2. 搜索段任务表述不清（深度 vs 联网）
3. 段落职责混合

解释：
模型在 prompt1 中优先完成“语义解释任务”，
在 prompt2 中优先满足“结构可判定约束”。

结论：
结构强约束显著降低 prompt drift。
